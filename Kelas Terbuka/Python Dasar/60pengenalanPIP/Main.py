# MENGENAL PIP

'''
    1. Cara lihat list pip (pip list) di cmd
    2. Cara lihat version pip (pip --version)
    3. Cara install library di pipy (pip install (tulis nama library))
    4. Cara install library di pipy berbagi versi (pip install (tulis nama library) == (version dari library))
    5. Cara uninstall library (pip uninstall (tulis nama library))
    6. Cara update/upgrade pip version (python.exe -m pip install --upgrade pip)
    
'''